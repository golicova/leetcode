#include <iostream> 
#include <vector> 

using namespace std; 

void rotate(vector<vector<int>>& matrix) {
    // take first element of each vector 
    // revrse it
    // add to a new vector 

    // check for matrix size 
    // that is how many vectors you will delete in the end 

    int vector_size = matrix.size(); 

    
}

int main() {
    

    return 0; 
}